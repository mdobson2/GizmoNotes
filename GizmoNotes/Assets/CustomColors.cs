using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CustomColors : MonoBehaviour {

    public static Color greenTransparent = new Color(0, 1, 0, .3f);

    public static Color puce = new Color(.8f, .533f, .6f);
}
